{% extends 'layouts/base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto mt-4 rounded shadow-lg">
                <h1 class="mt-5 mb-3 text-center">Réinitialiser votre mot de passe</h1>
            
            {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-error mb-3 rounded" role="alert">{{ flashError }}</div>
            {% endfor %}

            <div>
                    <p class="mb-5 text-center text-danger">
                        Entrer votre adresse Email pour recevoir le lien de restauration.
                    </p>
                </div>
    
            {{ form_start(requestForm) }}
                {{ form_row(requestForm.email) }}
                
                <button class="btn" style="background-color:#dc3545;color:white; margin-left:15%;" type="submit">Recevoir un mail pour restaurer votre mot de passe</button>
            {{ form_end(requestForm) }}
            <br>
    </div>
    </div>

{% endblock %}
